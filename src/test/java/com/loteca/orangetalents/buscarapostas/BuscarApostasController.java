package com.loteca.orangetalents.buscarapostas;

import java.util.Map;

import com.loteca.orangetalents.compartilhado.CustomMockMvc;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.request.MockHttpServletRequestBuilder;
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders;
import org.springframework.test.web.servlet.result.MockMvcResultMatchers;

// @SpringBootTest
// @AutoConfigureMockMvc
public class BuscarApostasController {

    // @Autowired
	// private MockMvc mockMvc;

    // @Autowired
    // CustomMockMvc mockMvcPost;

    // private String email = "email@email.com";

    // {
    //     mockMvcPost.post("/apostas", Map.of("email", email));
    // }

    // @Test
    // @DisplayName("Quando email válido retornar lista de apostas")
    // void test1() throws Exception{
    //     MockHttpServletRequestBuilder content = MockMvcRequestBuilders
    //         .get("/apostas")
    //         .param("email", "email@email.com")
    //         .accept(MediaType.APPLICATION_JSON_VALUE);
			
	// 	mockMvc.perform(content)
    //         .andExpect(MockMvcResultMatchers.status().is2xxSuccessful());
    // }
    
}
